# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
#
# For the purposes of this accelorator I have example.tfvars pushed to the repo.
# This is fine to omit from your repository as long as you are careful and are using private repositories.
# Use environment variables for Terraform to grap when authenticating to various providers as a best practice and never store secrets in these files.
# *.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore terraform provider version lock file
.terraform.lock.hcl